#include <stdio.h>
int main() {
   printf("Hello, World!");                 // printf() displays the string inside quotation
   printf("This is My First Repo");         // printf() displays the string inside quotation   
   return 0;
}
